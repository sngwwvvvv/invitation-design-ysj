# Shared UI Components

This repository is a single static HTML page. It has no component framework and no separate shared UI primitive files. Buttons, cards, headings, and sections are authored directly in `index.html` and styled by `styles.css`.
