# Task 1: Add a remote-draft contract validator and prove the current draft fails

Create `scripts/validate-invitation-draft.mjs`. It consumes a Superdesign draft ID as `process.argv[2]` and authenticated local Superdesign CLI credentials, and exits 0 with one PASS line per contract or 1 with one FAIL line per broken contract. Use Node built-ins and invoke `npx --yes @superdesign/cli@latest get-design --draft-id <id> --json`, parse `htmlContent`, and validate the approved main heading, five colors (`#0A2D54`, `#0063A6`, `#43A64E`, `#F0F4F8`, `#64748B`), removal of old palette (`#142755`, `#A9AABC`, `#35B84A`, `#F6F4EF`), unified `intro-section`, reflection background at 0.4 opacity, horizontal logo without filters, removal of date/place label, dedicated `event-details`, removal of celebration heading, exact account message, inline account information (`우리은행`, `049-087742-02-501`, `윤성중`), no modal behavior, copy button `copy-account-number`, digits-only clipboard value `04908774202501`, polite live-region success/failure feedback, and preserved map/profile content. Add a usage error with exit 2 when no draft ID is supplied. Run it against draft `09814ba2-f71a-4d01-a3be-9ae06580b185` and confirm RED, then commit with `test: validate revised invitation draft contract`.

Report file: `task-1-report.md`. Write a full report there; return only status, commit(s), one-line test summary, and concerns.
