# Task 2: Update the design system and upload the approved assets

Modify only `.superdesign/design-system.md` and use the existing local assets `img/hoyeon_logo_horizontal.png` and `img/reflection_background.png`. Update the information structure and component rules to state: `InvitationIntro` is one continuous section containing the horizontal logo, approved main heading, invitation message, and `#event-details` over `reflection_background.png` at 40% background-layer opacity; `ProfileSection` preserves the approved portrait and ten career entries; `MapSection` preserves the static map, address, transit row, Naver Map link, and parking notice; `AccountNotice` has no h2 and no modal, and shows the approved message plus inline account card with `#copy-account-number` and `aria-live="polite"`. Replace obsolete colors with exact tokens: `--deep-navy: #0A2D54`, `--standard-blue: #0063A6`, `--vivid-green: #43A64E`, `--platinum-cool-gray: #F0F4F8`, `--slate-steel: #64748B`. Remove rules requiring vertical logo, separate reflection card, dark navy cards, account modal, modal trigger, modal closing interactions, or placeholder account details. Verify old palette and forbidden rules are absent. Upload both PNG assets with `@superdesign/cli@latest upload-asset`, project ID `f01b42fd-67b3-410d-ab73-e875e40af1a0`, `--no-canvas --json`; preserve the returned public URLs in the report for Task 3. Commit only the design-system update with `design: update invitation palette and section rules`; do not stage image additions/deletions.

Report file: `task-2-report.md`. Return only status, commit hash, one-line verification/upload summary, asset URLs, and concerns.
